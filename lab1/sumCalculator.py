#  WAP that takes three no and prints their sum. Every no is given on a separate line.

numA = float(input("\nEnter the first no: "))
numB = float(input("\nEnter the second no: "))
numC = float(input("\nEnter the third no: "))

sumTotal = numA + numB + numC
print(sumTotal)
